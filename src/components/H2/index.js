import styled from 'styled-components';
import {visuallyHidden} from '../../styles/mixins';

export const InvisibleH2 = styled.h2`${visuallyHidden}`;

const H2 = styled.h2`
  color: #95C11F;
`;

export default H2;
